---
phase: 01-skills-compatibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - typescript-rules/skills/typescript-conventions/SKILL.md
  - jsdoc-standards/skills/jsdoc-conventions/SKILL.md
  - workflow-toolkit/skills/write-a-prd/SKILL.md
  - workflow-toolkit/skills/improve-codebase-architecture/SKILL.md
  - workflow-toolkit/skills/prd-to-issues/SKILL.md
  - workflow-toolkit/skills/tdd/SKILL.md
  - workflow-toolkit/skills/grill-me/SKILL.md
autonomous: true
requirements: [SKILL-01, SKILL-02, SKILL-03]
user_setup: []
must_haves:
  truths:
    - "OpenCode agent asked about TypeScript conventions returns guidance from typescript-rules skill"
    - "OpenCode agent asked about JSDoc conventions returns guidance from jsdoc-standards skill"
    - "OpenCode agent can load all 5 workflow-toolkit skills by name"
  artifacts:
    - path: "typescript-rules/skills/typescript-conventions/SKILL.md"
      provides: "TypeScript conventions skill with YAML frontmatter"
      contains: "paths:"
    - path: "jsdoc-standards/skills/jsdoc-conventions/SKILL.md"
      provides: "JSDoc conventions skill with YAML frontmatter"
      contains: "paths:"
    - path: "workflow-toolkit/skills/write-a-prd/SKILL.md"
      provides: "write-a-prd skill with YAML frontmatter"
      contains: "paths:"
    - path: "workflow-toolkit/skills/improve-codebase-architecture/SKILL.md"
      provides: "improve-codebase-architecture skill with YAML frontmatter"
      contains: "paths:"
    - path: "workflow-toolkit/skills/prd-to-issues/SKILL.md"
      provides: "prd-to-issues skill with YAML frontmatter"
      contains: "paths:"
    - path: "workflow-toolkit/skills/tdd/SKILL.md"
      provides: "tdd skill with YAML frontmatter"
      contains: "paths:"
    - path: "workflow-toolkit/skills/grill-me/SKILL.md"
      provides: "grill-me skill with YAML frontmatter"
      contains: "paths:"
  key_links:
    - from: "typescript-rules/skills/typescript-conventions/SKILL.md"
      to: "OpenCode auto-discovery (.claude/skills/)"
      via: "YAML frontmatter with name, description, paths fields"
      pattern: "paths:\\s*\\[.*\\.ts.*\\]"
    - from: "jsdoc-standards/skills/jsdoc-conventions/SKILL.md"
      to: "OpenCode auto-discovery (.claude/skills/)"
      via: "YAML frontmatter with name, description, paths fields"
      pattern: "paths:\\s*\\[.*\\.ts.*\\]"
---

<objective>
Add YAML frontmatter to all 7 existing SKILL.md files to enable OpenCode auto-discovery via `.claude/skills/` path.
</objective>

<execution_context>
@/Users/pauvelascogarrofe/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pauvelascogarrofe/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-skills-compatibility/01-CONTEXT.md
@typescript-rules/skills/typescript-conventions/SKILL.md
@jsdoc-standards/skills/jsdoc-conventions/SKILL.md
@workflow-toolkit/skills/write-a-prd/SKILL.md
@workflow-toolkit/skills/improve-codebase-architecture/SKILL.md
@workflow-toolkit/skills/prd-to-issues/SKILL.md
@workflow-toolkit/skills/tdd/SKILL.md
@workflow-toolkit/skills/grill-me/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add YAML frontmatter to typescript-rules and jsdoc-standards SKILL.md files</name>
  <files>typescript-rules/skills/typescript-conventions/SKILL.md, jsdoc-standards/skills/jsdoc-conventions/SKILL.md</files>
  <read_first>
    - typescript-rules/skills/typescript-conventions/SKILL.md (current state: has name and description, needs paths)
    - jsdoc-standards/skills/jsdoc-conventions/SKILL.md (current state: has name and description, needs paths)
    - .planning/phases/01-skills-compatibility/01-CONTEXT.md (D-02: Full frontmatter format, D-05: paths field patterns)
  </read_first>
  <acceptance_criteria>
    - typescript-rules/skills/typescript-conventions/SKILL.md contains `paths: ["*.ts", "*.tsx"]` after the description field
    - jsdoc-standards/skills/jsdoc-conventions/SKILL.md contains `paths: ["*.ts", "*.tsx", "*.js", "*.jsx"]` after the description field
    - Both files retain existing content (frontmatter is additive, body unchanged)
    - grep -c "paths:" typescript-rules/skills/typescript-conventions/SKILL.md returns 1
    - grep -c "paths:" jsdoc-standards/skills/jsdoc-conventions/SKILL.md returns 1
  </acceptance_criteria>
  <action>
    **Per D-02 and D-05 from CONTEXT.md:**
    
    1. Edit `typescript-rules/skills/typescript-conventions/SKILL.md`:
       - Current frontmatter has `name:` and `description:` fields
       - Add `paths: ["*.ts", "*.tsx"]` after the description field, before the closing `---`
       - Final frontmatter should be:
       ```yaml
       ---
       name: typescript-conventions
       description: >
         TypeScript coding conventions for any TS project. Covers types & interfaces,
         functions & error handling, imports, and naming conventions. Use when
         writing or reviewing TypeScript code.
       paths: ["*.ts", "*.tsx"]
       ---
       ```
    
    2. Edit `jsdoc-standards/skills/jsdoc-conventions/SKILL.md`:
       - Current frontmatter has `name:` and `description:` fields
       - Add `paths: ["*.ts", "*.tsx", "*.js", "*.jsx"]` after the description field, before the closing `---`
       - Final frontmatter should be:
       ```yaml
       ---
       name: jsdoc-conventions
       description: >
         TypeScript JSDoc documentation conventions with three enforcement levels
         (Minimal, Standard, Strict). This skill should be used when the user asks to
         "add JSDoc", "document this code", "add documentation", "write JSDocs",
         "improve documentation", "set JSDoc level", "which JSDoc level", or when
         applying JSDoc conventions to TypeScript code. Provides rules for tag usage,
         format, and level selection across any TypeScript project.
       paths: ["*.ts", "*.tsx", "*.js", "*.jsx"]
       ---
       ```
    
    **Important:** Only modify the YAML frontmatter section. Do NOT change any content after the closing `---`.
  </action>
  <verify>
    <automated>grep -c "paths:" typescript-rules/skills/typescript-conventions/SKILL.md && grep -c "paths:" jsdoc-standards/skills/jsdoc-conventions/SKILL.md</automated>
  </verify>
  <done>Both typescript-rules and jsdoc-standards SKILL.md files have paths: field in YAML frontmatter, enabling OpenCode auto-discovery per SKILL-01 and SKILL-02.</done>
</task>

<task type="auto">
  <name>Task 2: Add YAML frontmatter to all 5 workflow-toolkit SKILL.md files</name>
  <files>workflow-toolkit/skills/write-a-prd/SKILL.md, workflow-toolkit/skills/improve-codebase-architecture/SKILL.md, workflow-toolkit/skills/prd-to-issues/SKILL.md, workflow-toolkit/skills/tdd/SKILL.md, workflow-toolkit/skills/grill-me/SKILL.md</files>
  <read_first>
    - workflow-toolkit/skills/write-a-prd/SKILL.md (current frontmatter state)
    - workflow-toolkit/skills/improve-codebase-architecture/SKILL.md (current frontmatter state)
    - workflow-toolkit/skills/prd-to-issues/SKILL.md (current frontmatter state)
    - workflow-toolkit/skills/tdd/SKILL.md (current frontmatter state)
    - workflow-toolkit/skills/grill-me/SKILL.md (current frontmatter state)
    - .planning/phases/01-skills-compatibility/01-CONTEXT.md (D-02: Full frontmatter format, D-05: paths field patterns)
  </read_first>
  <acceptance_criteria>
    - All 5 workflow-toolkit SKILL.md files contain `paths:` field in YAML frontmatter
    - write-a-prd gets `paths: ["*.md", "*.txt"]` (PRD/documentation files)
    - improve-codebase-architecture gets `paths: ["*.ts", "*.tsx", "*.js", "*.jsx"]` (code analysis)
    - prd-to-issues gets `paths: ["*.md", "*.txt"]` (issue/documentation files)
    - tdd gets `paths: ["*.ts", "*.tsx", "*.js", "*.jsx"]` (test files)
    - grill-me gets `paths: ["*.md", "*.txt"]` (design/plan files)
    - grep -c "paths:" workflow-toolkit/skills/*/SKILL.md returns 1 for each file (5 total)
  </acceptance_criteria>
  <action>
    **Per D-02 and D-05 from CONTEXT.md:**
    
    Add `paths:` field to each workflow-toolkit SKILL.md file. All files already have `name:` and `description:` fields.
    
    1. `workflow-toolkit/skills/write-a-prd/SKILL.md`:
       - Add `paths: ["*.md", "*.txt"]` after description, before closing `---`
    
    2. `workflow-toolkit/skills/improve-codebase-architecture/SKILL.md`:
       - Add `paths: ["*.ts", "*.tsx", "*.js", "*.jsx"]` after description, before closing `---`
    
    3. `workflow-toolkit/skills/prd-to-issues/SKILL.md`:
       - Add `paths: ["*.md", "*.txt"]` after description, before closing `---`
    
    4. `workflow-toolkit/skills/tdd/SKILL.md`:
       - Add `paths: ["*.ts", "*.tsx", "*.js", "*.jsx"]` after description, before closing `---`
    
    5. `workflow-toolkit/skills/grill-me/SKILL.md`:
       - Add `paths: ["*.md", "*.txt"]` after description, before closing `---`
    
    **Format for each file** (example with write-a-prd):
    ```yaml
    ---
    name: write-a-prd
    description: >
      This skill should be used when the user asks to "write a PRD", "create a
      product requirements document", "plan a new feature", "write requirements",
      "draft a spec", "spec out a feature", "define requirements", or wants to
      plan and define what to build before building it.
    paths: ["*.md", "*.txt"]
    ---
    ```
    
    **Important:** Only modify the YAML frontmatter section. Do NOT change any content after the closing `---`.
  </action>
  <verify>
    <automated>grep -l "paths:" workflow-toolkit/skills/*/SKILL.md | wc -l</automated>
  </verify>
  <done>All 5 workflow-toolkit SKILL.md files have paths: field in YAML frontmatter, enabling OpenCode auto-discovery per SKILL-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SKILL.md files → OpenCode/Cursor agents | Skill files are read by agents; no user input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | SKILL.md files | accept | Static markdown files, no execution |
| T-01-02 | Information Disclosure | SKILL.md content | accept | Intended to be read by agents; no sensitive data |
</threat_model>

<verification>
- [ ] `grep -c "paths:" typescript-rules/skills/typescript-conventions/SKILL.md` returns 1
- [ ] `grep -c "paths:" jsdoc-standards/skills/jsdoc-conventions/SKILL.md` returns 1
- [ ] `grep -l "paths:" workflow-toolkit/skills/*/SKILL.md | wc -l` returns 5
- [ ] OpenCode agent can load typescript-conventions skill by name
- [ ] OpenCode agent can load jsdoc-conventions skill by name
- [ ] OpenCode agent can load all 5 workflow-toolkit skills by name
</verification>

<success_criteria>
1. An OpenCode agent asked about TypeScript conventions returns guidance sourced from the typescript-rules skill (confirming auto-discovery via `.claude/skills/`)
2. An OpenCode agent asked about JSDoc conventions returns guidance sourced from the jsdoc-standards skill
3. An OpenCode agent working on any workflow task can load all 5 workflow-toolkit skills by name without a file path
</success_criteria>

<output>
After completion, create `.planning/phases/01-skills-compatibility/01-01-SUMMARY.md`
</output>
