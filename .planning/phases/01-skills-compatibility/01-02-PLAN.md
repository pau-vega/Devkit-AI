---
phase: 01-skills-compatibility
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - .cursor/skills/typescript-rules/SKILL.md
  - .cursor/skills/jsdoc-standards/SKILL.md
  - .gitignore
  - scripts/build-marketplace.sh
autonomous: true
requirements: [CUR-SKILL-01, CUR-SKILL-02]
user_setup: []
must_haves:
  truths:
    - "Cursor agent in project with .cursor/skills/typescript-rules/ returns TypeScript guidance via /typescript-rules invocation"
    - "Cursor agent loads jsdoc-standards from .cursor/skills/jsdoc-standards/SKILL.md with paths: frontmatter scoping to JS/TS files"
  artifacts:
    - path: ".cursor/skills/typescript-rules/SKILL.md"
      provides: "Symlink to typescript-rules skill with Cursor frontmatter"
      contains: "paths:"
    - path: ".cursor/skills/jsdoc-standards/SKILL.md"
      provides: "Symlink to jsdoc-standards skill with Cursor frontmatter"
      contains: "paths:"
    - path: ".gitignore"
      provides: "Ensures .cursor/ is not committed to repo"
      contains: ".cursor/"
    - path: "scripts/build-marketplace.sh"
      provides: "Build script that generates .cursor/skills/ symlinks"
      contains: "ln -s"
  key_links:
    - from: ".cursor/skills/typescript-rules/SKILL.md"
      to: "typescript-rules/skills/typescript-conventions/SKILL.md"
      via: "symlink (generated by build script)"
      pattern: "ln -s.*typescript-conventions.*typescript-rules"
    - from: ".cursor/skills/jsdoc-standards/SKILL.md"
      to: "jsdoc-standards/skills/jsdoc-conventions/SKILL.md"
      via: "symlink (generated by build script)"
      pattern: "ln -s.*jsdoc-conventions.*jsdoc-standards"
---

<objective>
Create .cursor/skills/ directory structure with symlinks for typescript-rules and jsdoc-standards, add .cursor/ to .gitignore, and update build script to generate symlinks automatically.
</objective>

<execution_context>
@/Users/pauvelascogarrofe/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pauvelascogarrofe/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-skills-compatibility/01-CONTEXT.md
@scripts/build-marketplace.sh
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .cursor/skills/ symlinks for typescript-rules and jsdoc-standards</name>
  <files>.cursor/skills/typescript-rules/SKILL.md, .cursor/skills/jsdoc-standards/SKILL.md, typescript-rules/skills/typescript-conventions/SKILL.md, jsdoc-standards/skills/jsdoc-conventions/SKILL.md</files>
  <read_first>
    - .planning/phases/01-skills-compatibility/01-CONTEXT.md (D-01: symlinks from .cursor/skills/<name>/ → skills/, D-04: build script generates symlinks, .cursor/ is gitignored)
    - typescript-rules/skills/typescript-conventions/SKILL.md (source file for symlink)
    - jsdoc-standards/skills/jsdoc-conventions/SKILL.md (source file for symlink)
    - .planning/quick/260506-612-cursor-opencode-compat-research/260506-612-RESEARCH.md (Cursor Agent Skills standard, paths: field requirement)
  </read_first>
  <acceptance_criteria>
    - .cursor/skills/typescript-rules/ directory exists (created via mkdir -p)
    - .cursor/skills/jsdoc-standards/ directory exists (created via mkdir -p)
    - .cursor/skills/typescript-rules/SKILL.md is a symlink pointing to ../../typescript-rules/skills/typescript-conventions/SKILL.md
    - .cursor/skills/jsdoc-standards/SKILL.md is a symlink pointing to ../../jsdoc-standards/skills/jsdoc-conventions/SKILL.md
    - readlink .cursor/skills/typescript-rules/SKILL.md returns ../../typescript-rules/skills/typescript-conventions/SKILL.md
    - readlink .cursor/skills/jsdoc-standards/SKILL.md returns ../../jsdoc-standards/skills/jsdoc-conventions/SKILL.md
    - Both SKILL.md files contain paths: field (from Plan 01)
  </acceptance_criteria>
  <action>
    **Per D-01 and D-04 from CONTEXT.md:**
    
    1. Create .cursor/skills/ directory structure:
    ```bash
    mkdir -p .cursor/skills/typescript-rules
    mkdir -p .cursor/skills/jsdoc-standards
    ```
    
    2. Create symlinks from .cursor/skills/<name>/SKILL.md to the canonical skills/ directory:
    ```bash
    # From .cursor/skills/typescript-rules/ directory:
    # Target: ../../typescript-rules/skills/typescript-conventions/SKILL.md
    ln -s ../../typescript-rules/skills/typescript-conventions/SKILL.md .cursor/skills/typescript-rules/SKILL.md
    
    # From .cursor/skills/jsdoc-standards/ directory:
    # Target: ../../jsdoc-standards/skills/jsdoc-conventions/SKILL.md
    ln -s ../../jsdoc-standards/skills/jsdoc-conventions/SKILL.md .cursor/skills/jsdoc-standards/SKILL.md
    ```
    
    3. Verify symlinks work:
    ```bash
    cat .cursor/skills/typescript-rules/SKILL.md | head -15  # Should show YAML frontmatter with paths:
    cat .cursor/skills/jsdoc-standards/SKILL.md | head -15  # Should show YAML frontmatter with paths:
    ```
    
    **Important:** The symlinks use relative paths (../../) because .cursor/skills/ and the plugin directories are sibling directories under the repo root.
  </action>
  <verify>
    <automated>readlink .cursor/skills/typescript-rules/SKILL.md && readlink .cursor/skills/jsdoc-standards/SKILL.md && grep -c "paths:" .cursor/skills/typescript-rules/SKILL.md && grep -c "paths:" .cursor/skills/jsdoc-standards/SKILL.md</automated>
  </verify>
  <done>Symlinks created for typescript-rules and jsdoc-standards under .cursor/skills/, both SKILL.md files readable with paths: frontmatter for Cursor discovery per CUR-SKILL-01 and CUR-SKILL-02.</done>
</task>

<task type="auto">
  <name>Task 2: Add .cursor/ to .gitignore and update build script to generate symlinks</name>
  <files>.gitignore, scripts/build-marketplace.sh</files>
  <read_first>
    - .gitignore (current state)
    - scripts/build-marketplace.sh (current state, needs symlink generation logic)
    - .planning/phases/01-skills-compatibility/01-CONTEXT.md (D-04: build script generates symlinks, .cursor/ is gitignored; Specific Ideas section has symlink logic example)
  </read_first>
  <acceptance_criteria>
    - .gitignore contains .cursor/ entry (with trailing slash)
    - scripts/build-marketplace.sh contains function to create .cursor/skills/ symlinks
    - Build script creates .cursor/skills/typescript-rules/ symlink
    - Build script creates .cursor/skills/jsdoc-standards/ symlink
    - Running bash scripts/build-marketplace.sh does not error on symlink creation
    - grep -c ".cursor/" .gitignore returns 1
  </acceptance_criteria>
  <action>
    **Per D-04 from CONTEXT.md and Vercel Labs pattern:**
    
    1. Add .cursor/ to .gitignore:
    ```bash
    echo ".cursor/" >> .gitignore
    ```
    
    2. Update scripts/build-marketplace.sh to generate .cursor/skills/ symlinks:
    
    Add a new function before the `main()` function:
    ```bash
    # Generate .cursor/skills/ symlinks for Cursor discovery
    generate_cursor_symlinks() {
      local repo_root="$1"
      
      # Create .cursor/skills/ directory
      mkdir -p "$repo_root/.cursor/skills/typescript-rules"
      mkdir -p "$repo_root/.cursor/skills/jsdoc-standards"
      
      # typescript-rules: symlink from .cursor/skills/typescript-rules/ to skills/typescript-conventions/
      ln -sf "$repo_root/typescript-rules/skills/typescript-conventions/SKILL.md" \
         "$repo_root/.cursor/skills/typescript-rules/SKILL.md"
      
      # jsdoc-standards: symlink from .cursor/skills/jsdoc-standards/ to skills/jsdoc-conventions/
      ln -sf "$repo_root/jsdoc-standards/skills/jsdoc-conventions/SKILL.md" \
         "$repo_root/.cursor/skills/jsdoc-standards/SKILL.md"
      
      echo "Created .cursor/skills/ symlinks for typescript-rules and jsdoc-standards" >&2
    }
    ```
    
    3. Call the function in `main()` before the plugin discovery loop:
    ```bash
    main() {
      # ... existing validation ...
      
      # Generate .cursor/skills/ symlinks for Cursor compatibility
      generate_cursor_symlinks "$REPO_ROOT"
      
      # ... rest of existing main() ...
    }
    ```
    
    **Important:** Use `ln -sf` (force) so the build script is idempotent (can be run multiple times without errors).
  </action>
  <verify>
    <automated>grep -c ".cursor/" .gitignore && grep -c "generate_cursor_symlinks" scripts/build-marketplace.sh && bash scripts/build-marketplace.sh 2>&1 | head -20</automated>
  </verify>
  <done>.cursor/ added to .gitignore and build script generates symlinks for typescript-rules and jsdoc-standards automatically per CUR-SKILL-01 and CUR-SKILL-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .cursor/skills/ symlinks → Source SKILL.md | Symlinks reference files within repo; no external trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-------------|-------------|-----------------|
| T-01-03 | Tampering | Symlinks in .cursor/skills/ | accept | Symlinks point to internal files; gitignore prevents commit |
| T-01-04 | Information Disclosure | SKILL.md content | accept | Intended to be read by Cursor agents; no sensitive data |
</threat_model>

<verification>
- [ ] `readlink .cursor/skills/typescript-rules/SKILL.md` returns valid path
- [ ] `readlink .cursor/skills/jsdoc-standards/SKILL.md` returns valid path
- [ ] `grep -c ".cursor/" .gitignore` returns 1
- [ ] `grep -c "generate_cursor_symlinks" scripts/build-marketplace.sh` returns 1
- [ ] Cursor agent can load typescript-rules via /typescript-rules invocation
- [ ] Cursor agent loads jsdoc-standards from .cursor/skills/jsdoc-standards/SKILL.md with paths: frontmatter
</verification>

<success_criteria>
4. A Cursor agent in a project with `.cursor/skills/typescript-rules/` returns the same TypeScript guidance via `/typescript-rules` invocation
5. A Cursor agent loads jsdoc-standards from `.cursor/skills/jsdoc-standards/SKILL.md` with `paths:` frontmatter scoping it to JS/TS files
</success_criteria>

<output>
After completion, create `.planning/phases/01-skills-compatibility/01-02-SUMMARY.md`
</output>
