---
quick_id: 260506-612
type: plan
date: 2026-05-06
phase: quick
plan: 01
wave: 1
depends_on: []
autonomous: true
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
  - .planning/STATE.md
requirements: [scope-extension-cursor]
must_haves:
  truths:
    - ROADMAP title and all four phase Goals/Success Criteria mention both OpenCode and Cursor where applicable
    - REQUIREMENTS.md contains new CUR-* REQ-IDs mapped to the existing four phases per the research compatibility matrix
    - PROJECT.md Core Value explicitly references both runtimes (OpenCode + Cursor)
    - STATE.md reflects new dual-runtime scope and records this quick task as completed
    - Capability gaps are documented honestly (Cursor: no portable subagent file; OpenCode: no prompt-based hooks)
  artifacts:
    - path: .planning/ROADMAP.md
      provides: dual-runtime roadmap with Cursor deltas integrated into existing 4 phases
      contains: "Dual-Runtime Compatibility (OpenCode + Cursor)"
    - path: .planning/REQUIREMENTS.md
      provides: extended REQ-ID set including CUR-SKILL-*, CUR-CMD-*, CUR-HOOK-*, CUR-MKTPL-*
      contains: "CUR-"
    - path: .planning/PROJECT.md
      provides: updated project overview mentioning both runtimes
      contains: "Cursor"
    - path: .planning/STATE.md
      provides: updated state with dual-runtime scope and quick-task log
      contains: "Cursor"
  key_links:
    - from: .planning/ROADMAP.md
      to: .planning/REQUIREMENTS.md
      via: REQ-ID references in each phase's Requirements field
      pattern: "CUR-(SKILL|CMD|HOOK|MKTPL)-[0-9]+"
    - from: .planning/REQUIREMENTS.md
      to: .planning/quick/260506-612-cursor-opencode-compat-research/260506-612-RESEARCH.md
      via: each CUR-* REQ traces to a row in the Component Compatibility Matrix
      pattern: ".cursor/"
---

<objective>
Extend the project's planning artifacts (ROADMAP.md, REQUIREMENTS.md, PROJECT.md, STATE.md) from "OpenCode compatibility" to "dual-runtime compatibility (OpenCode + Cursor)" using the locked research findings at `.planning/quick/260506-612-cursor-opencode-compat-research/260506-612-RESEARCH.md`.

Purpose: The research established that Cursor parity is mostly free (skills + hook scripts reuse verbatim) and recommended Option (a) — integrate Cursor deltas into the existing 4 phases instead of appending phases 5-8. The planning artifacts must reflect this new scope before any execution work begins.

Output: Four updated markdown files under `.planning/`. No source code, no plugin file changes, no new directories. Doc edits only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/quick/260506-612-cursor-opencode-compat-research/260506-612-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update REQUIREMENTS.md and ROADMAP.md to dual-runtime scope</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>
Update both files to reflect dual-runtime (OpenCode + Cursor) scope per research recommendation Option (a). Do NOT split phases — keep 4 phases, add Cursor deltas inside each.

**REQUIREMENTS.md edits:**

1. Change title to `# Requirements: Dual-Runtime Compatibility (OpenCode + Cursor)`.
2. Update Core Value line to: "Developers using OpenCode or Cursor get the same real-time convention enforcement and on-demand review experience as Claude Code users."
3. Keep all 15 existing REQ-IDs unchanged. Add the following new Cursor REQ-IDs in the appropriate sections:

   In **Skills Compatibility** section (Phase 1):
   - `CUR-SKILL-01`: typescript-rules ships `.cursor/skills/typescript-rules/SKILL.md` (or symlink/copy from canonical `skills/`) with `name`, `description`, and `paths:` frontmatter
   - `CUR-SKILL-02`: jsdoc-standards ships `.cursor/skills/jsdoc-standards/SKILL.md` with same frontmatter shape
   - `CUR-SKILL-03`: workflow-toolkit ships all 5 SKILL.md files under `.cursor/skills/<name>/` discoverable by Cursor

   In **Commands Port** section (Phase 2):
   - `CUR-CMD-01`: `/ts-review` available in Cursor via a Skill with `disable-model-invocation: true` (Cursor's substitute for slash commands per research §Cursor Commands)
   - `CUR-CMD-02`: `/jsdoc-review` available in Cursor via the same disable-model-invocation skill pattern
   - `CUR-CMD-03`: `/create-workflow` available in Cursor via the same pattern

   Note in **Agents Port** section: Cursor has NO portable subagent file format (Custom Modes are UI-only). The reviewer-agent role is absorbed into the command-skill body in Cursor. AGNT-01 and AGNT-02 remain Claude Code + OpenCode only — add an explicit note "Cursor: capability gap — no portable agent file. Reviewer prompt inlined into CUR-CMD-01/02 skill body."

   In **Hook Enforcement** section (Phase 3):
   - `CUR-HOOK-01`: typescript-rules ships `.cursor/hooks.json` (copy of existing `hooks.json` with `PreToolUse` → `preToolUse`, `Bash` matcher → `Shell`, `version: 1` added) plus the existing 7 shell scripts placed under `.cursor/hooks/scripts/` and reused verbatim
   - `CUR-HOOK-02`: jsdoc-standards `.cursor/hooks.json` ships the same way; existing JSDoc validation scripts reused verbatim; existing `"type": "prompt"` JSDoc hook ports to Cursor verbatim with added `timeout`
   - `CUR-HOOK-03`: Subagent hook coverage in Cursor empirically validated (analog to OpenCode #5894) — log `subagentStart` / `preToolUse` interaction once before enforcement ships
   - Note: existing HOOK-04 (jq fallback for OpenCode) is NO LONGER NEEDED — research §OpenCode Cross-Check confirms OpenCode does not pipe stdin to shell scripts. Update HOOK-04's text to: "Existing `.sh` scripts continue to read `tool_input.file_path` (snake_case) — works for both Claude Code AND Cursor. No fallback needed; OpenCode uses programmatic camelCase `output.args.filePath` in its TypeScript plugin instead."

   In **Marketplace UI** section (Phase 4):
   - `CUR-MKTPL-01`: `marketplace.html` shows Cursor compatibility badges on all three plugin cards (alongside Claude Code and OpenCode badges) — three columns total
   - `CUR-MKTPL-02`: `marketplace.html` shows Cursor-specific install instructions (copy `.cursor/` into project root) distinct from Claude Code (`--plugin-dir`) and OpenCode (manual file copy)
   - `CUR-MKTPL-03`: `scripts/build-marketplace.sh` discovers `.cursor/` components in addition to `.opencode/` and reflects them in the regenerated page

4. Update **v2 Requirements** section: PBH-01 note that Cursor DOES support prompt-based hooks (so PBH-01 is OpenCode-only, not a v2 gap for Cursor). PBH-02 unchanged (still OpenCode-specific).

5. Update **Out of Scope** table: add row "Portable Cursor subagent file — Cursor's Custom Modes are UI-only; reviewer prompts inline into command-skills instead."

6. Update **Traceability** table: append new CUR-* REQ-IDs with their phase mapping (CUR-SKILL-* → Phase 1; CUR-CMD-* → Phase 2; CUR-HOOK-* → Phase 3; CUR-MKTPL-* → Phase 4).

7. Update Coverage summary: v1 requirements now total 15 + 12 = 27 (or final count after adds). Make sure the displayed totals match the new count.

8. Update footer dates to `*Last updated: 2026-05-06 to extend scope to dual-runtime (OpenCode + Cursor) per research 260506-612*`.

**ROADMAP.md edits:**

1. Change title to `# Roadmap: Dual-Runtime Compatibility (OpenCode + Cursor)`.
2. Update Core Value line to match new PROJECT.md/REQUIREMENTS.md wording.
3. Update top-level **Phases** checklist so each phase line mentions both runtimes where applicable, e.g.:
   - Phase 1: "All SKILL.md files are auto-discovered and readable by OpenCode AND Cursor agents"
   - Phase 2: "On-demand review commands and agents work in OpenCode (full) and Cursor (commands via disable-model-invocation skills; agents inline — no portable subagent file)"
   - Phase 3: "Real-time convention enforcement fires on write/edit in OpenCode (TypeScript plugin) and Cursor (`.cursor/hooks.json` reusing existing shell scripts verbatim)"
   - Phase 4: "Landing page advertises tri-runtime compatibility (Claude Code + OpenCode + Cursor) with three install paths"
4. Update the **Overview** table:
   - Phase 1 Requirements: `SKILL-01..03, CUR-SKILL-01..03`
   - Phase 2 Requirements: `CMD-01..03, AGNT-01..02, CUR-CMD-01..03`
   - Phase 3 Requirements: `HOOK-01..04, CUR-HOOK-01..03`
   - Phase 4 Requirements: `MKTPL-01..03, CUR-MKTPL-01..03`
5. Update **Phase Details** for each of the 4 phases:
   - Goal: rewrite to explicitly cover both runtimes (and where Cursor differs structurally, name the difference)
   - Requirements: add the new CUR-* IDs
   - Success Criteria: add Cursor-specific criteria mirroring the OpenCode ones (e.g., "Running `/ts-review` typed in Cursor invokes the disable-model-invocation skill and produces the same Error/Warning/Suggestion report")
   - For Phase 2, add an explicit "Capability gap" line: "Cursor has no portable subagent file format. AGNT-01 and AGNT-02 ship for Claude Code + OpenCode only. Cursor users get equivalent UX via the command-skill (CUR-CMD-01/02) which inlines the reviewer prompt."
   - For Phase 3, add an explicit "Capability gap" line: "OpenCode has no prompt-based LLM hooks (#20387). The existing `"type": "prompt"` JSDoc hook ships for Claude Code + Cursor only. OpenCode users get equivalent coverage via the on-demand `/jsdoc-review` command."
6. Update **Coverage** table at bottom: append all CUR-* REQ-IDs with their phase mapping. Update the "All N v1 requirements mapped" sentence to reflect the new total.
7. Update footer to `*Created: 2026-05-05* / *Updated: 2026-05-06 — extended scope to dual-runtime (OpenCode + Cursor) per research quick task 260506-612*`.

**Editing rule:** Use the Edit tool for surgical changes — do not rewrite either file from scratch. Preserve existing formatting (table alignment, blank lines between sections, checkbox style). When in doubt, mirror the existing OpenCode-row style for new Cursor rows.
  </action>
  <verify>
    <automated>grep -c "CUR-SKILL-01\|CUR-CMD-01\|CUR-HOOK-01\|CUR-MKTPL-01" .planning/REQUIREMENTS.md .planning/ROADMAP.md && grep -q "Dual-Runtime Compatibility (OpenCode + Cursor)" .planning/ROADMAP.md && grep -q "Dual-Runtime Compatibility (OpenCode + Cursor)" .planning/REQUIREMENTS.md && grep -q "capability gap" .planning/ROADMAP.md -i && grep -q "no prompt-based" .planning/ROADMAP.md -i</automated>
  </verify>
  <done>
    REQUIREMENTS.md and ROADMAP.md both show new title, both contain all 12 new CUR-* REQ-IDs distributed correctly across the 4 phases per research §Component Compatibility Matrix, and both honestly call out the two capability gaps (Cursor=no portable agents; OpenCode=no prompt hooks). Existing 15 REQ-IDs are preserved unchanged. Coverage / Traceability tables updated to include new IDs. No phases added or split.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update PROJECT.md and STATE.md to reflect dual-runtime scope</name>
  <files>.planning/PROJECT.md, .planning/STATE.md</files>
  <action>
Update the two remaining planning artifacts to mirror the scope extension landed in Task 1.

**PROJECT.md edits:**

1. Update H1 title from `# AI-Devkit — OpenCode Compatibility` to `# AI-Devkit — Dual-Runtime Compatibility (OpenCode + Cursor)`.
2. Update **What This Is** paragraph: change "extend the marketplace so all three plugins work equally well inside OpenCode sessions" to "extend the marketplace so all three plugins work equally well inside OpenCode AND Cursor sessions". Add one sentence: "Cursor was added to scope on 2026-05-06 after research (`.planning/quick/260506-612-cursor-opencode-compat-research/`) showed Cursor parity is mostly free — its hook system is a near-clone of Claude Code's, and existing shell scripts can be reused verbatim."
3. Update **Core Value** to: "Developers using OpenCode or Cursor get the same real-time convention enforcement and on-demand review experience as Claude Code users."
4. Update **Requirements > Active** list: change each "OpenCode" bullet to "OpenCode and Cursor" where it makes sense (e.g., "typescript-rules plugin adapted to work in OpenCode and Cursor (hooks, command, agent where supported, skill)"). Note explicitly on the agents bullet that "Cursor lacks a portable agent file — reviewer prompt inlines into the command-skill instead." Add bullets:
   - "[ ] Cursor's hook/skill/command equivalents researched and documented (DONE 2026-05-06 — see `.planning/quick/260506-612-cursor-opencode-compat-research/`)"
   - "[ ] marketplace.html shows tri-runtime install instructions (Claude Code + OpenCode + Cursor)"
5. Update **Constraints** section: keep all existing constraints. Add: "Cursor compatibility files live under each plugin's `.cursor/` namespace, mirroring `.opencode/` and `.claude-plugin/`. No existing files are modified to add Cursor support."
6. Update **Key Decisions** table: add one row:
   `| Cursor included in scope | Research 2026-05-06 showed near-zero porting cost: skills reuse, hook scripts reuse verbatim, only command shape changes (skills with disable-model-invocation) | Pending — to be implemented across phases 1-4 |`
7. Update footer to `*Last updated: 2026-05-06 — extended scope to dual-runtime (OpenCode + Cursor) per research quick task 260506-612*`.

**STATE.md edits:**

1. Update **Project Reference** date to `(updated 2026-05-06)`.
2. Update **Core value** line to match the new dual-runtime wording.
3. Keep **Current Phase** as "Phase 1 — Skills Compatibility" but rename to "Phase 1 — Skills Compatibility (OpenCode + Cursor)".
4. Update **Phases** table to rename each phase to its dual-runtime form (e.g., "Skills Compatibility (OpenCode + Cursor)", "Commands and Agents Port (OpenCode + Cursor where supported)", "Hook Enforcement Port (OpenCode + Cursor)", "Marketplace UI and Install Docs (tri-runtime)").
5. Update **Performance Metrics** counts:
   - "Phases complete: 0/4" stays
   - "Requirements complete: 0/N" — N is the new total after Task 1's REQ additions (15 + 12 = 27, but verify against the actual count Task 1 produced)
6. Update **Accumulated Context > Key Decisions** list: append:
   - "Cursor support added to scope 2026-05-06 — research (`.planning/quick/260506-612-cursor-opencode-compat-research/`) showed Cursor's hook system is a near-clone of Claude Code's, allowing zero-edit reuse of existing shell scripts"
   - "Cursor capability gap accepted: no portable subagent file — reviewer-agent role inlines into command-skills"
   - "OpenCode HOOK-04 jq-fallback no longer needed — research re-verified that OpenCode does not pipe stdin to shell scripts; field-name fallback was a third-party plugin artifact"
7. Update **Known Risks** list: append:
   - "Cursor subagent hook coverage unverified — `subagentStart` / `preToolUse` interaction must be empirically validated before Phase 3 ships (analog to OpenCode #5894)"
8. Update **Session Continuity**:
   - Last activity: "2026-05-06 — scope extended to dual-runtime (OpenCode + Cursor) via quick research task 260506-612 and corresponding planning-artifact updates"
   - Next action: keep as "`/gsd-plan-phase 1`" (Phase 1 still next; it now covers both runtimes)
9. Add a new section **Quick Tasks** at the end (before any final separator) listing this completion:
   ```
   ## Quick Tasks
   | Date | ID | Type | Description | Outcome |
   |------|----|----|-------------|---------|
   | 2026-05-06 | 260506-612 | research + plan | Cursor + OpenCode compatibility research and scope extension | RESEARCH.md + PLAN.md produced; ROADMAP/REQUIREMENTS/PROJECT/STATE updated to dual-runtime |
   ```

**Editing rule:** Use the Edit tool for surgical changes. Preserve existing table formatting and section ordering. Don't drop unchanged content.
  </action>
  <verify>
    <automated>grep -q "Dual-Runtime Compatibility (OpenCode + Cursor)" .planning/PROJECT.md && grep -q "OpenCode or Cursor" .planning/PROJECT.md && grep -q "OpenCode or Cursor" .planning/STATE.md && grep -q "260506-612" .planning/STATE.md && grep -q "Cursor capability gap" .planning/STATE.md -i && grep -q "Quick Tasks" .planning/STATE.md</automated>
  </verify>
  <done>
    PROJECT.md and STATE.md both reflect dual-runtime scope. Core Value mentions both runtimes in both files. STATE.md records the quick-task completion (260506-612) under a new Quick Tasks section, lists the Cursor scope-add in Key Decisions, and notes the unverified Cursor subagent-hook coverage as a known risk. Phase names in STATE.md match ROADMAP.md phase names. No existing decisions, risks, or phase entries are deleted — only added or rewritten in place.
  </done>
</task>

</tasks>

<verification>
After both tasks:
- `grep -l "OpenCode + Cursor" .planning/*.md` returns ROADMAP.md, REQUIREMENTS.md, PROJECT.md, STATE.md (all four)
- `grep -c "CUR-" .planning/REQUIREMENTS.md` returns ≥ 12 (one per new REQ + traceability rows)
- `grep -c "CUR-" .planning/ROADMAP.md` returns ≥ 12 (phase requirements + coverage rows)
- All four files still have intact YAML/markdown structure (no broken tables, no orphaned bullets)
- No source code, no plugin files, no `.cursor/` or `.opencode/` directories created — doc edits only
- Phase count unchanged at 4
- Both capability gaps documented honestly (Cursor agents, OpenCode prompt hooks)
</verification>

<success_criteria>
1. ROADMAP.md title is "Roadmap: Dual-Runtime Compatibility (OpenCode + Cursor)" and each of the 4 phases has Cursor-specific Goal language, Cursor REQ-IDs, and Cursor success criteria
2. REQUIREMENTS.md contains 12 new CUR-* REQ-IDs (3 skills + 3 commands + 3 hooks + 3 marketplace) mapped per research §Component Compatibility Matrix, plus the HOOK-04 wording correction
3. PROJECT.md Core Value reads "Developers using OpenCode or Cursor get the same real-time convention enforcement and on-demand review experience as Claude Code users."
4. STATE.md records the quick-task completion (260506-612), updates Performance Metrics totals, lists Cursor scope-add in Key Decisions, and surfaces the Cursor subagent-hook unknown as a Known Risk
5. Both capability gaps appear in writing in both ROADMAP.md and REQUIREMENTS.md: (a) Cursor — no portable subagent file format; (b) OpenCode — no prompt-based LLM hooks (#20387)
6. No source code or plugin files modified; no new directories created under any plugin; phase count remains 4
</success_criteria>

<output>
After completion, the four updated planning artifacts under `.planning/` are the deliverable. No SUMMARY file required for quick mode — execution simply lands the edits and commits via the standard quick-mode flow.
</output>
