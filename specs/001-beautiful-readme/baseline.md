# Baseline: README before beautification

**Captured**: 2026-06-05
**Feature**: 001-beautiful-readme

## Size

- Bytes: `10224`
- Budget: `< 30000`

## Section inventory (16 H2 headings, in document order)

| # | Heading | Notes |
| --- | --- | --- |
| 1 | `## Install` | Must remain. |
| 2 | `## What the installer prompts` | Must remain. |
| 3 | `## Where files land` | Must remain. |
| 4 | `## Known limitations` | Must remain. |
| 5 | `## Flags` | Must remain. |
| 6 | `## Releasing (maintainer note)` | Must remain. |
| 7 | `## Overview` | Appears twice (once per plugin). Both must remain. |
| 8 | `## Features` | Appears twice (once per plugin). Both must remain. |
| 9 | `## Installation` | Appears twice (once per plugin). Both must remain. |
| 10 | `## Usage` | Appears twice (once per plugin). Both must remain. |

Plugin sub-READMEs share the H1 "typescript-rules" / "jsdoc-standards" with the marketplace README; this is the marketplace README only.

## Content-preservation contract

Every heading above MUST still exist (verbatim or in a more specific
form) in the rewritten `README.md`. The change is visual
re-organization and embellishment, not content removal.
